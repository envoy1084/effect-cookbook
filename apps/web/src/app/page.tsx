import { Button } from "@effect-cookbook/ui/components/button";

const Home = () => {
  return (
    <div>
      <Button variant="default">Hello World</Button>
    </div>
  );
};

export default Home;
